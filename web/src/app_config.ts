
export const app_config = {
  // leave empty if the API is on the same host as the web app
  api_host_url: '',
  config_path: '/api/config',
  info_path: '/api/info',
  signalk_path: '/api/signalk',
};
